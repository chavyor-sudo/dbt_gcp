version: 2
models:
- name: raw_data
  config:
    meta:
      data_contract: bronze-orders-contract
  columns:
  - name: order
    data_tests:
    - dbt_expectations.dbt_expectations.expect_column_values_to_be_of_type:
        column_type: null

