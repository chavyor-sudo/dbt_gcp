dataContractSpecification: "3.0.2" # הגרסה העדכנית של התקן
kind: DataContract                 # <--- השדה הקריטי שהיה חסר והכשיל את הריצה!
id: "bronze-orders-contract"
version: "1.0.0"

info:
  title: "Bronze Orders Data Contract"
  description: "Data contract defining bronze orders structure"
  owner: "chavy-team"

models:
  stg_order:
    type: table
    fields:
      order_id:
        type: string
        required: true
        primary: true
      user_id:
        type: string
        required: true
      amount:
        type: number
        required: true
