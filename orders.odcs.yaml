dataContractSpecification: 1.1.0
id: bronze-orders-contract
info:
  title: Bronze Orders Data Contract
  version: 1.0.0
servers:
  production:
    type: bigquery
models:
  stg_order:
    type: table
    fields:
      order_id:
        type: string
        required: true
        unique: true
      user_id:
        type: string
        required: true
      amount:
        type: decimal
