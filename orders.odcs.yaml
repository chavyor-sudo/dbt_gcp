dataContractSpecification: "2.2.0" # שימוש במספר גרסה ישיר במקום קישור URL מלא
id: bronze-orders-contract
version: 1.0.0
info:
  title: Bronze Orders Data Contract
  owner: chavy-team

models:
  stg_order:
    type: table
    fields:
      order_id:
        type: string
        required: true
        unique: true
      user_id:
        type: string
        required: true
      amount:
        type: float
        required: true
