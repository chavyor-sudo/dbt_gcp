datacontract: "3.0.0"
id: "bronze-orders-contract"
version: "1.0.0"

info:
  title: "Bronze Orders Data Contract"
  description: "Provides verified bronze order data for analytics and reporting"
  owner: "chavy-team"

sources:
  raw_data: # שם ה-Source המדויק שקוד ה-SQL שלך מחפש!
    description: "Raw source database zone"
    tables:
      order: # שם הטבלה האמיתית שלכם
        description: "Bronze managed table under contract protection"
        columns:
          order_id:
            type: "string"
            description: "Primary key order id"
            required: true
            unique: true
          user_id:
            type: "string"
            description: "User reference"
            required: true
          amount:
            type: "number"
            description: "Total transaction amount"
            required: true
